#include	"all.h"

void kidrawol320( st320* pst, double t, palyaleiro* pleiro, int view,
		int timeki, double baljobb, double ugrasnagysag, int ugras1volt,
		double forgas ) {
	error( "No kene itt lennie (kidrawol320)!" );
	pst++;
	t++;
	pleiro++;
	view++;
	timeki++;
	baljobb++;
	ugrasnagysag++;
	ugras1volt++;
	forgas++;
}

void teljes( void ) {
	error( "No kene itt lennie (teljes)!" );
}

